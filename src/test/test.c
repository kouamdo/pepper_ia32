#include "../../include/test.h"
#include <stdint.h>

uint8_t find_unit_test(const char* unit_test, test_case_result test_case)
{
    uint8_t i = 0;

    while (test_case.tests_units->test_unit_name) {
        /* code */
    }
}